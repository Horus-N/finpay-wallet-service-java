server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/finpay_wallet_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true